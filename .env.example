# Copy to .env and fill values. Never commit real secrets.
DATABASE_URL="file:./dev.db"
AUTH_SECRET="change-me-to-a-long-random-string"

# Delivery: mock | live
DELIVERY_MODE="mock"
# WHATSAPP_API_TOKEN=
# SMTP_HOST=
# SMTP_USER=
# SMTP_PASS=

# GitHub (use Git Credential Manager — never paste PAT in chat/files)
# GITHUB_TOKEN=
