# Copy to .env and fill values. Never commit real secrets.
DATABASE_URL="file:./dev.db"

# GitHub (use `gh auth login` or Git Credential Manager — never paste PAT in chat/files)
# GITHUB_TOKEN=

# Future integrations
# SEFAZ_CERT_PATH=
# WHATSAPP_API_TOKEN=
# PAYMENT_PROVIDER_KEY=
